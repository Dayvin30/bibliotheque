package com.octest.test;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

import com.octest.beans.Exemplaire;

class testExemplaire {
/*
	@Test
	void testBibliothèque() {
		
		Exemplaire mesExemplaires = new Exemplaire();
		mesExemplaires.setBibliotheque("Bibliothèque de Paris");
		mesExemplaires.setId(0);
		mesExemplaires.setIsbn(15030);
		
		assertEquals("Bibliothèque de Paris", mesExemplaires.getBibliotheque());
	}

	@Test
	void testId() {
		
		Exemplaire mesExemplaires = new Exemplaire(); 

		mesExemplaires.setId(0);

		
		assertEquals(0, mesExemplaires.getId());
	}
	@Test
	void testIsbn() {
		
		Exemplaire mesExemplaires = new Exemplaire();


		mesExemplaires.setIsbn(15030);
		
		assertEquals(15030, mesExemplaires.getIsbn());
   }
*/
}
